import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    itemDetailContainer: {
        width: "90%",
        margin: "auto"
    },
    notFoundMensaje:{
        fontFamily: "Coda",
        textAlign: "center",
        fontWeight: "bold",
        color: "#8D0A0A"
    }
});

export default useStyles;